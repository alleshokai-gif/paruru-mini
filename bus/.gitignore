# Local secrets (the empty template is the only exception).
.dev.vars
.dev.vars.*
!.dev.vars.example

# Local validation runtime.
.venv/
__pycache__/
*.pyc
node_modules/
.wrangler/
dist/
.local/

# Server-only generated Static. Never publish to Pages or commit real feed extracts.
generated/*.json
generated/*.tmp
