# PALURU Bus P0 実データ検証記録

## 朝の位置調査再開（2026-09-11、実施前契約）

07:00 JSTに保存済み資料を確認。最新観測要約は前夜23:26、朝の追加観測結果は見つからなかった。停止原因は未確認。Cloud Runのコード/ローカル受入は前夜までの状態を保持し、本ターンは位置調査を優先する。

- 問題：前夜の比較は溝16の1便・停留所付近中心で、4方向と移動中の証拠が不足。
- 方針：P0の4方向について公式Navi参照JSONとODPTを同じ取得窓で最大8回、31秒間隔で観測する。静的GTFSは必要な対象tripの全停留所座標/順序とshape有無を1回確認する。
- 位置の主根拠：ODPT GPS・vehicle timestamp・連続した座標変化。GTFS trip識別は便の所属判定に使うが、VP sequence/stop_id/statusは補助情報のみ。Navi contentは答え合わせ専用で位置推定器の入力にしない。
- 便の照合：NaviとGTFSの共通trip IDは未確認。系統/行先/方向/座標で近い候補を出しても自動確定しない。同じ系統の複数車両、timestamp欠損、座標欠損、対応差が大きいものを明示する。
- 影響範囲：調査用script、Git除外の要約、DATA_VALIDATION。Core/Adapter/PWA/Position flag、本番URL、deploy設定は変更しない。内部endpointを本番へ追加しない。HTMLスクレイピングはしない。
- 保存：API全文は保存せず必要なP0便の位置/時刻/候補だけ。サンプルごとに安全な要約を保存して、途中停止しても取得済み証拠を失わない。前夜の要約は上書きしない。
- 検証/rollback：観測スクリプトの構文と秘密混入、4方向カバレッジ、GPS欠損/曖昧性を確認。研究ファイルはruntime/image対象外で、停止しても本番影響なし。判定が十分でなければnull/Feature Gate OFFを維持する。

### 朝の実施結果（2026-09-11 07:03〜07:12 JST）

**途中：4方向で朝のVP/TUを追加確認。位置P1は証拠が増えたが、表示解禁の判断には不足。Position UIはOFF。**

第1窓は07:03:44.720〜07:07:27.369、第2窓は07:09:02.061〜07:12:44.841。それぞれ8サンプル、合計16サンプル。各窓では1つのODPT feedと4つの参照JSONを並行要求し、全要求の受信窓は0.571〜0.881秒。Naviの車両観測timestampは今回も取得できておらず、「同じ要求窓」はデータ生成時刻の同期を意味しない。

第2窓は帰り便の発車後を追うため、Navi参照条件だけを神木本町→菅生車庫、神木本町→鷲ヶ峰営業所前へ変更した。ODPTは元のP0 4方向を満たすtripだけを抽出する。参照先の便すべてが登戸/溝口発であるとは扱わない。取得要約にreferenceFrom/Toを残した。固定お気に入りは増やしていない。

|P0方向|VPの異なるtrip数（第1窓 / 第2窓）|今回詳しく追った実便|確認できたGPSの進行|
|---|---|---|---|
|神木本町→登戸|5 / 6|`4112_01_100001279`、登05、神木07:09予定|緑地→初山→平付近→平〜堰下の区間候補→向丘出張所/平橋/向丘中学校下付近|
|神木本町→溝口駅南口|29 / 31|`4112_01_100001774`、溝15、神木07:04予定|向丘出張所→平橋付近→平橋〜向丘中学校下の区間候補→向丘中学校下付近|
|登戸→神木本町|3 / 3|`4112_01_100001277`、登05、登戸07:07予定|始発付近→登戸〜宿河原小学校前の区間候補→宿河原小学校前付近|
|溝口駅南口→神木本町|13 / 14|`4112_01_100002670`、溝18、3番07:06予定|始発→高津区役所前→上之橋→下作住宅前→宮ノ下の各停留所付近|

trip所属はStatic/RT識別子で確認。Naviとの照合は系統・行先/経由・方向・運行時刻・座標移動による候補比較で、共通trip_idによるJOINではない。例えばStatic有効headsign `菅生車庫(蔵敷)`に対しNaviはdestination `菅生車庫`、transit `蔵敷`。単純完全一致では取りこぼす。一方、系統だけで近いGPSを採用すると溝18の「溝口駅南口」と「第三京浜入口（溝口経由）」を取り違える候補が現れた。**最近傍での自動便確定は行わない。**

### GPSと参照contentの具体例

距離は球面距離であり、道路距離や同一車両IDの証明ではない。停留所座標と順序は同一Static版から別途抽出した。VP sequence/stop_idは区間候補の主根拠に使っていない。

|受信JST / 便末尾|ODPT位置timestamp JST|ODPT lat / lon|Navi content / 座標差|GPSから確認した範囲|
|---|---|---|---|---|
|07:06:55.601 / 1279|07:05:58|35.59904861 / 139.57215881|5 / 0.17m|直前は平付近約3m。今回点は平266.94m・堰下132.57m。平→堰下方向の移動中候補|
|07:05:52.362 / 1774|07:04:48|35.59995270 / 139.57887268|2 / 134.94m|直前の平橋付近6.09mから離れ、平橋55.82m・向丘中学校下140.76m。次サンプルは向丘中学校下6.63m|
|07:06:23.953 / 1774|07:05:25|35.60034561 / 139.58029175|1 / 5.79m|向丘中学校下付近。参照はわずかに先へ進む|
|07:10:06.347 / 1277|07:09:29|35.61910248 / 139.57119751|7 / 0.07m|登戸198.65m・宿河原小学校前425.83m。連続GPSで駅を離れて次停留所方向へ進行|
|07:11:09.745 / 1277|07:10:29|35.61791992 / 139.57261658|7 / 0.14m|登戸382.34m・宿河原小学校前242.14m。次サンプルは宿河原小学校前1.92m|
|07:11:09.745 / 2670|07:10:43|35.59979248 / 139.60014343|6 / 0.49m|下作住宅前5.99m。前サンプルは上之橋付近、後続は宮ノ下付近|

溝口発の別便`4112_01_100002468`も07:06:23に高津区役所前81.04m・溝口駅南口85.12mの中間候補を観測。ただしこの瞬間の対応Naviマーカーはなく、同時答え合わせには数えない。**4方向すべての途中区間を公式表示まで同時照合できたとは言えない。**

### 公式画面の位置表示

Chromeで神木本町→登戸の接近一覧と、通常の「走行位置」リンクから登05走行図（routeLayoutCd=2405）を確認。HTMLを取得してデータ化する処理は作っていない。

- 07:07表示の接近一覧：神木07:09予定便は「5個前」「約5分」。第1窓のcontentは7→6→6→5→5→5→5→4。
- 07:07の走行図：登戸行き「04分待ち」のアイコンが平と堰下の間の行に表示。07:06:55のGPS/content例と近接する時刻帯。スクリーンショット1回はtimeout、再取得は成功。
- 07:10表示の走行図：登戸行き「01分待ち」が平橋と向丘中学校下の間に表示。
- 07:11台の走行図を実画面画像で再確認：登戸行き「01分待ち」は向丘中学校下と神木本町の間、菅生車庫行きは登戸と宿河原小学校前の間に表示。第2窓の07:11:09/41でGPS・contentを取得した。

画面は停留所間の離散的なアイコン行で、道路上の精密座標ではない。ブラウザ再読込とJSON/ODPT要求は原子的キャプチャではなく、秒単位の完全同期は未達。少なくとも「平付近→平/堰下の間」「登戸付近→宿河原小学校前へ」の表示と連続GPSの方向は整合する。現在位置の自動判定器を検証した結果ではない。

### 不一致・欠損・更新周期

- **近いGPSでもcontentが同じとは限らない。** 登戸行き1279は07:11:09、向丘中学校下9.27mでcontent=2。32秒後、同停留所2.66mでcontent=1。GPSの停留所付近判定だけでは「到着」と「通過」を一意に区別できない。
- 同便07:06:55は平〜堰下の途中候補かつcontent=5。補助sequence=9と乗車sequence=13の単純差は4となり参照と不一致。sequence/statusは比較資料に留める。
- 登戸行き1279で同時要求のNavi/VP差は0.17mまで合う一方、07:04:17は393m、07:05:20は288mずれた。溝口発2670も07:10:06で521.20m差。Naviが先へ進んだ点が後のODPTに現れる例があり、受信時刻を位置時刻として扱わない。
- 第1窓のVP ageは概ね50〜110秒、第2窓は概ね20〜77秒。ただし神木→溝口の選択範囲には古いVPも残り、最大1215.93秒。新しいfeedに古い車両位置が含まれうる。
- 神木→溝口の4trip（末尾2166/2173/2169/2282）にGPS欠損、合計28観測行（第1窓18、第2窓10）。0座標に補完しない。母集団は予定時刻が観測開始の30分前〜60分後に入る便であり、表示可能な次便だけではない。
- 選択VPのtimestamp欠損0、同日tripのTU結合欠損0。ただし「TUがある」と「乗車stopの未来departure予測がある」は別。全便でETAが出るとは判定しない。
- feed timestamp差：第1窓32/32/31/32/31/32/32秒、第2窓31/32/31/32/31/32/32秒。登戸行き1279の第1窓の車両timestamp差は0/62/18/49/0/60/0秒。Naviサーバー更新周期は未確認で、こちらの31秒間隔と混同しない。
- 参照JSONは全要求成功、切捨て上限到達なし。始発を参照した第1窓の帰り2方向は各3回マーカー0件。発車済み便の消失を非対応やODPT欠損と解釈しない。

### 保存・テスト・次の境界

`observe-position-p0.js`は8回で停止し、サンプルごとに一時ファイル→renameで保存。`--return-reference`で上記参照条件だけ切替。`extract-position-static.py <capture JSON> <observed trip IDs>`は観測済みIDに限定して最大24便、今回10便・99標柱を抽出し、既存出力は上書き拒否。同一Static `20260701_20260828`の10便すべてshape_id空、shape座標0件だった。全線ZIP/全テーブルは保存していない。

要約は`bus/generated/position-p0-20260911T070344.json`、`position-p0-20260911T070902.json`、前者の`-static.json`（49,631 bytes）、`position-p0-morning-analysis.json`。すべてGit除外・Docker/Cloud Build送信対象外。analysisの候補距離順は同一便確定ではなく、位置推定器ではない。

- 今回のコード変更は調査用2scriptのみ。Core/Adapter/UI/flag、GAS、本番PWA設定の変更なし。
- Bus tests 28/28 PASS。初回の2件はesbuildの親ディレクトリ読取拒否で失敗し、同じ試験を許可された通常権限で再実行してPASS。試験内容は変更していない。
- Repository `node --test test/*.test.js`：84/84 PASS。
- ローカルNode本番設定のHTTP実データ試験：07:12:27、4方向×3便、HTTP 200。RTあり/なし混在、health、prod CORS、過去ETA抑止、position OFF、cache hit時のODPT再fetchなしをPASS。登戸発の次3便は今回static fallback。Cloud Run実環境/Android受入とは別。
- 次は位置P1の設計レビュー用に、全停留所座標とtrip順序、GPS timestamp/連続点、接近/離脱、方向/折返し、曖昧時nullの判定契約を整理する。shapeなしの停留所間直線を実道路として採用しない。内部Navi contentを判定入力や本番依存にしない。
- **現時点は位置P1の自動表示NO-GO、研究用の区間候補照合は前進。P0は位置OFFでCloud Run準備を継続できる。** remote build/Run検証、本番deploy、Android実機受入は未実施。

## 市バスナビ参照調査（2026-09-10 23:00〜26 JST・前夜の履歴）

本節は前夜の記録で、最新結果は上の朝の節を優先する。**市バスナビ内部JSONは検証の参照専用。本番候補にしない。** HTMLスクレイピングの実装・保存はしていない。公式画面を通常のブラウザ操作で読み、そこで参照された公開JSと限定的なJSON応答を確認した。本番AdapterはODPTだけを使う。

### 内部通信の確認範囲と公開区分

ブラウザ操作ツールにはNetwork/HAR取得機能がないため、XHRの実際の送信履歴・ヘッダー・レスポンスをDevToolsで捕捉したとは扱わない。公開JSで通信定義を確認し、その定義と画面で観測済みの停留所条件による独立HTTP照会でJSONを確認した。ブラウザのJSON URL直接表示はクライアントにブロックされ、ブラウザ経由の本文取得には使えなかった。

|通信先（同ホスト`kcbn.bus-navigation.jp`）|確認根拠・データ|区分|
|---|---|---|
|`GET /wgsys/wgp/busMarkImg.htm`|位置地図の公開JSにGET/JSON定義。独立照会で200 `application/json;charset=UTF-8`、`mapDataList`実データを取得|画面内部用。公開利用の根拠未確認。参照限定|
|`POST /wgsys/wgp/showInfoBetStops.htm`|`busApproachPC.js`にJSON、fromSignKey/toSignKey/routePatternCd、返却`stop`/`time`の読取定義。実応答未取得|画面内部用と推定。RT位置情報を返すとは未確認|
|`POST /wgsys/wgp/checkRollsignNmByTransAndFrom.htm`|公開JSで路線画面遷移の前処理。応答をカンマ区切りとして解釈。実応答未取得|画面内部用|
|`/wgp/bus.htm`、`/wgp/route.htm`、`/wgp/busApproachPCMap.htm`|フォーム遷移・画面更新。図式の走行位置画面を通常操作で確認|HTML画面。データ取得Adapterにはしない|
|ODPT Static/TU/VP|既存の正規APIとGTFS公開。今回も正規キーで取得|採用中の公式公開データ|

根拠JS： [接近画面](https://kcbn.bus-navigation.jp/wgsys/js/wgp/busApproachPC.js)、[位置地図](https://kcbn.bus-navigation.jp/wgsys/js/wgp/busPositionDispPC.js)、[走行図](https://kcbn.bus-navigation.jp/wgsys/js/wgp/busRoute.js)。位置地図JSは13,084 bytes、取得200。254行付近でJSON照会、132行付近で受信済み`content`を表示する。公開JS自体がGPSから停留所数を計算している証拠はない。

[公式案内](https://www.city.kawasaki.jp/820/page/0000039449.html)・[市バスナビ注意事項](https://kcbn.bus-navigation.jp/wgsys/wgp/notes.htm?locale=ja)を確認。リンク許可はAPI再利用の許可とは別。確認したページから内部endpointの外部アプリ利用を認める公開API契約・ライセンスは見つからなかった。「APIが存在しない」「利用禁止が確定」とまでは結論しない。本番には採用しない。

### 実際のJSON項目

- 検索入力：locale、from/to、fromType/toType、各lat/lng、fromSignpoleKey、existYn。位置地図JSの定義を確認。
- 実応答：`pageLoadTime`はHH:mm。`mapDataList`のtype `1`が出発停留所、`2`が到着停留所、`3`が今回の車両マーカー。車両に`lat`/`lng`、`content`、rollsignName、destinationName、transitName、signpoleKeyFrom/Toがあった。
- `content`は文字列。`0`は始発待ちの表示、その他は値＋通過表示文言へ使う公開JS定義。実数値3/2と、画面の「3個前」「2個前」相当を比較する参照にする。GTFS sequenceと同じ定義であるとは扱わない。
- 応答ルートのキー集合にbusCode / routePatternCode / targetOpStatus / autoRefresh等があることと、車両ごとのtrip ID・status・timestampが提供されることは別。**GTFS trip_idとの共通ID、車両位置timestamp、車両ETA/delay、previous/next stopは今回確認できなかった。** 不明な値は補わない。
- 独立照会23:17:57はMizo→神木の`mapDataList=[]`。0台を「データ非対応」と解釈しない。発車後の同じP0便を神木で観測するため、参照画面だけ神木→鷲ヶ峰営業所前を使用した。P0設定・対象4方向は増やしていない。

### 便候補と同時観測

ODPTで観測した便：`4112_01_100002060`、route `10034`（溝１６）、service `O_4112_01`、2026-09-10、溝口駅南口 `434_2` 23:17 → 神木本町 `184_3` 23:24（Static）。始発予定/系統/犬蔵経由/行先/標柱が公式画面と一致した。内部JSONに共通trip_idがないため、**Naviとの同一便は有力な候補照合。識別子による完全JOINは未証明**。

23:15の公式画面は始発待ち・まもなく発車、23:15:46のODPT VPはSTOPPED_AT、緯度35.59918594/経度139.61029053。TU departureは23:17:00、delay0。23:17:57ではVP statusがIN_TRANSIT_TOに変わるが、座標は同値、sequence1/stop434_2のままだった。これは補助項目の非同時性を調べる材料であり、出発/区間の主根拠にしない。

23:23:01の参照JSONは溝16の`content=5`、35.599283/139.596960。ODPT VPは35.59923172/139.59724426、timestamp23:22:20。公式接近画面23:23は5個前・約4分で発車。別の溝18は1個前であり、混同しない。

次の3件はNavi JSONとODPTを同時に要求し、各0.17〜0.27秒で両方受信した。画面23:25も溝16・計画23:24・3個前と照合した。位置図は23:11に溝18の停留所列/車両マーカーを確認したが、下表の溝16と同一便・同時刻の走行図キャプチャではない。下表と同時の「道路上のマーカー位置」を画面画像まで含めて照合する試験は残る。

|受信JST|Navi content|Navi lat/lon|ODPT VP lat/lon|ODPT位置timestamp JST|座標差（概算）|補助sequence/status/stop|
|---|---|---|---|---|---|---|
|23:25:05.181|3|35.600747 / 139.591410|35.60074615 / 139.59140015|23:24:27|0.90m|7 / STOPPED_AT / 81_2|
|23:25:36.367|3|35.601285 / 139.588893|35.60074615 / 139.59141541|23:24:48|235.79m|8 / INCOMING_AT / 231_2|
|23:26:07.613|2|35.601285 / 139.588893|35.60128403 / 139.58889771|23:25:24|0.44m|8 / STOPPED_AT / 231_2|

距離は両座標の球面距離であり、車両を別IDから同定する証明ではない。中段はNavi座標が森林公園前付近まで進む一方、ODPT座標が上作団地前付近に残る。Naviの座標が同じままcontent3→2へ変わることも観測。**座標・通過イベント・受信時刻は同期していると仮定できない。** Naviの車両観測timestampは未確認、ODPTの受信時点ageは38.18/48.37/43.61秒。

### ODPTのみで位置を再現するための証拠と不足

同じ公式Static版から対象1tripの全27停留所・座標を別途メモリ内確認。対象tripのshape_idは空、shape距離も空。P0の小型Staticは中間停留所を保持していないので、位置P1のgeometry資料としては不足している。

|停留所|stop ID|緯度 / 経度|GPSによる今回の観測|
|---|---|---|---|
|上作延|82_2|35.600034 / 139.593893|前方の順序確認用|
|上作団地前|81_2|35.600723 / 139.591350|23:25:05のODPT座標から5.21m、23:25:36も6.45m|
|森林公園前|231_2|35.601249 / 139.588941|23:26:07のODPT座標から5.52m|
|とのした橋|307_2|35.601309 / 139.585901|神木の前停留所|
|神木本町|184_3|35.600749 / 139.581875|今回の位置参照対象|

sequence/stop_idを位置判定に使わず、GPSから「上作団地前付近→森林公園前付近」の進行を対応付けられる証拠は得た。ただし観測点は停留所付近に偏り、**両停留所間を走行する途中の位置を一意に再構成できる実証には足りない**。道路shapeなしで隣接停留所を直線補間しない。時刻差、連続GPS、標柱の向き、並行道路/折返し、GPS欠落時のnull、4方向の中間区間サンプルを追加する。内部JSONは答え合わせ専用。**Position P1は検証継続、UI OFF。**

### 周期・保存・再現

- 3サンプルのODPT feed timestamp差は31秒/30秒（23:24:27→23:24:58→23:25:28）。車両timestamp差は21秒/36秒。以前のfeed約31〜32秒と同程度だが、車両別の座標更新が同周期とは証明できない。
- 参照JSONは能動的に約31秒間隔で取得。これはNaviサーバー更新周期の測定値ではない。今回確認した公開JSの更新操作はフォーム再送信。Naviの自動更新実周期は未確認。
- `node scripts/observe-position-reference.js <観測済みP0 trip_id>` は3回×31秒に制限。`generated/position-reference-summary.json`へ必要な位置要約だけ保存（Git/upload/image対象外）。レスポンス全文は保存しない。
- `generated/position-trip-summary.json`は同一tripの必要な停留所座標要約。元ZIP/全テーブルは保存していない。Worker/Core/PWAのどこからもこれらの調査用ファイルをimportしない。
- 判定：**Cloud Run移行は位置OFFのまま進める。位置再現GO-Aは未達。** 他3方向・同時刻の道路/走行図表示・GPS欠落/方向判定・近接して走る複数便の曖昧性は未検証。23時台の登戸方面は公式画面に60分内接近便がなかった。

## 以下は初回ODPT検証とその後の追補の履歴

> **以下の初回実データ結果は9節（2026-09-10）。正規キーでStatic・RTを取得し、4方向の便結合を実証した。静的次便は4方向で成立。未来の発車ETAは初回時点で3方向を確認。その後、登戸発も追加観測し、ユーザーは位置OFFのP0着手を許可した。位置調査の現在値は冒頭節を優先する。**
>
> 以下1〜8節と直下の箇条書きは2026-09-07時点の履歴。キー未設定による停止は解消済み。今回のコード・実測値・残る制限は9節を優先する。

- 記録日: 2026-09-07、Asia/Tokyo
- 状態: **途中。検証準備・合成試験まで。ODPT本体は未取得**
- 判定: **実データのGO判定は保留／本番P0実装開始はNO-GO（証拠不足）**
- 阻害要因: `ODPT_ACCESS_TOKEN` が未設定。`--check-config` は `ODPT_ACCESS_TOKEN_MISSING` で停止した。API拒否・データ非提供を観測したわけではない。
- 実データ観測日時・サンプル数・更新周期: **未観測**。未取得を0件・0秒として扱わない。
- 設計正本: [PALURU_BUS_P0_DESIGN.md](PALURU_BUS_P0_DESIGN.md)
- 今回の範囲: `bus/` の秘密管理・最小観測ツール・合成試験、設計/検証Markdown。本番UI・Provider・Worker・deployは対象外。

## 1. 使用する公式データと取得状態

2026-09-07にカタログで次の公開を再確認した。認証付きZIP/Protobufの内容確認とは別の証拠である。

| データ | 公式公開元 | 今回の本体取得 |
| --- | --- | --- |
| Static GTFS/GTFS-JP | [川崎市バス時刻表カタログ](https://ckan.odpt.org/dataset/transportation_bureau_city_of_kawasaki_all_lines)、表示版 `20260828` | 未実施 |
| TripUpdates | [川崎市バスRTカタログ](https://ckan.odpt.org/dataset/odpt_transportation_bureau_city_of_kawasaki_all_lines)、[TripUpdatesリソース](https://ckan.odpt.org/dataset/odpt_transportation_bureau_city_of_kawasaki_all_lines/resource/dc831adb-163f-4383-abdc-419d88a7b5f6) | 未実施 |
| VehiclePosition | 同RTカタログ、[VehiclePositionリソース](https://ckan.odpt.org/dataset/odpt_transportation_bureau_city_of_kawasaki_all_lines/resource/058d7e67-ca78-46a3-ab6b-9d425c7018e3) | 未実施 |

APIのホストは `api.odpt.org`。キーなしの取得先パスを検証スクリプトの定数として管理する。

```text
/api/v4/files/odpt/TransportationBureau_CityOfKawasaki/AllLines.zip
/api/v4/gtfs/realtime/odpt_TransportationBureau_CityOfKawasaki_AllLines_trip_update
/api/v4/gtfs/realtime/odpt_TransportationBureau_CityOfKawasaki_AllLines_vehicle
```

静的版は調査時版を引数 `--static-date` で指定する。本番で `20260828` を永久固定する設計ではない。
カタログの「一体となったデータ」の説明だけで両RTリソースが同一内容とは判断しない。実際のentity種別・件数・hashを観測する。
今回はAlertを追加取得しない。TUの運休/スキップ等は観測するが、Alertを含む本番の運休・迂回受入は未実施。

### 利用条件と秘密管理

- 採用経路は、川崎市交通局がODPTで公開した正規API。HTMLスクレイピングや市バスナビ内部通信へのfallbackは実装しない。
- カタログの適用ライセンスは [公共交通オープンデータ基本ライセンス](https://developer.odpt.org/terms/data_basic_license.html)。[センター利用規約](https://developer.odpt.org/terms/center_use_rules.html)・[開発者ガイドライン](https://developer.odpt.org/terms/data_basic_use_guideline.html)を含む初回調査は設計書5節に記録済み。
- 正規キー使用の指示は受領済み。キーの登録状況やアカウント個別の条件は今回確認していない。正規APIの存在を根拠に、あらゆる取得頻度・再配布方法が許可されるとは判定しない。
- `bus/.dev.vars` はローカル専用。値なしの `bus/.dev.vars.example` のみGit対象とする。`.dev.vars` と派生ファイルは `bus/.gitignore` で除外し、Git未追跡も確認した。
- スクリプトはキーをコマンド引数で受け取らず、値・認証付きURL・例外本文・レスポンス本文を出力しない。HTTP redirectは追従しない。失敗は安全なコードだけ出して停止する。
- 全線ZIP/RTレスポンスはメモリ内で解析し、全文を保存しない。証拠は対象停留所/便の必要部分と集計だけ。車両識別子は証拠出力から除外する。
- 本番キーはWorker Secretで管理する。本フェーズでCloudflare Secret作成・deploy・Git commit/pushは行っていない。

## 2. 4方向の正式ID・のりば

**GTFSの正式IDは未確定。** 公式バスナビで確認した画面用 `signPoleKey` を `stop_id` として流用しない。
名称は候補抽出だけに使用する。tripの停留所順・乗降可否・座標・親stop・`platform_code` の照合まで必要。

| 方向 | 正式 boarding stop_id / のりば | 正式 alighting stop_id | route_id / route名 | trip_id / stop_sequence |
| --- | --- | --- | --- | --- |
| 神木本町→登戸方面 | 未観測 | 未観測 | 未観測 | 未観測 |
| 神木本町→溝の口駅南口方面 | 未観測 | 未観測 | 未観測 | 未観測 |
| 登戸→神木本町 | 未観測 | 未観測 | 未観測 | 未観測 |
| 溝の口駅南口→神木本町 | 未観測 | 未観測 | 未観測 | 未観測 |

公式バスナビでの名称は「登戸駅（生田緑地口）」「溝口駅南口」。GTFSでの正式表記は未確認。
同名の全標柱を一括で乗車対象にせず、同じtrip内で乗車stop→降車stopの順に通る組合せだけを証拠に残す。
往復、経由違い、同一stopの再訪、駅の乗車側/降車側を区別する。帰りは「神木本町」の終着headsignだけを検索しない。
`platform_code` が欠損する場合は欠損のまま記録し、IDの末尾や座標から番号を作らない。

## 3. TripUpdates・VehiclePositionの充足状況

| 方向 | TU対象trip / 乗車stop直接更新 | time / delayの内訳 | VP→TU→Static結合 | stopsAway / 区間 | 有効な次便 |
| --- | --- | --- | --- | --- | --- |
| 神木本町→登戸 | 未観測 | 未観測 | 未観測 | 未観測 | 未観測 |
| 神木本町→溝口駅南口 | 未観測 | 未観測 | 未観測 | 未観測 | 未観測 |
| 登戸→神木本町 | 未観測 | 未観測 | 未観測 | 未観測 | 未観測 |
| 溝口駅南口→神木本町 | 未観測 | 未観測 | 未観測 | 未観測 | 未観測 |

実取得後は以下を更新する。

- TU: `trip_id`、`start_date`、`start_time`、`route_id` の存在率、対象便数、対象乗車stopの直接更新数、`stop_id` / `stop_sequence`、arrival/departure別の `time` / `delay` の存在率。
- VP: `trip_id`等、`current_stop_sequence`、`current_status`、`stop_id`、lat/lon、timestamp。フィールド未設定とProtobufの既定値を区別する。
- 便照合: `FeedEntity.id` で結合しない。運行日・便インスタンスの一意性を検証する。頻度運行、同一ID重複、日付不一致、経路不一致は未解決として残す。
- `futureDepartureCandidate` は観測用の時刻条件。鮮度の受入とは別であり、このフラグだけでUI表示可能/GOとは判定しない。

## 4. Staticとの時刻結合

算式の合成試験は実施したが、川崎市バス実データでの成立は未確認。
同じ便・運行日・乗車stop・イベント（arrivalまたはdeparture）を使用する。[GTFS-RT Reference](https://gtfs.org/documentation/realtime/reference/)

| 項目 | 検証方法 | 現状 |
| --- | --- | --- |
| scheduled | stop_timesの当該イベント時刻＋運行日。calendar/calendar_datesを解決 | 合成試験のみ |
| estimated | TUのtimeを優先。time欠損・delayありならscheduled＋delay | 合成試験のみ |
| delay | estimated−scheduled。同イベントの配信delayがあれば一致を検算 | 合成試験のみ |
| etaMinutes | `ceil((estimated−now)/60)`。負の差は過去イベントとして除外 | 合成試験のみ |

静的時刻・運行日はAsia/Tokyo。計算はepoch秒、証拠出力は `+09:00` 付きISO。
`24:xx` 以降は元の運行日から翌暦日へ換算する。arrivalの遅れをdepartureの遅れと混同しない。
配信delayとtimeの矛盾、片方だけの配信、両方欠損をそれぞれ記録する。欠損delayを0にしない。
定刻を過ぎた遅延便は、RTの未来のestimatedが有効なら時刻条件で残す。すでに過ぎたestimatedを「あと0分」で残さない。

## 5. 「何停前」「停留所間」の再構成

実データでの可否は**未確認**。今回の合成試験で検証した定義は次のとおり。

- 生の `target stop_sequence − current_stop_sequence` を `rawSequenceDelta` として保持する。
- 停留所列をsequence順に並べ、欠番がある場合は整数の差ではなく配列内の区間数を数える。
- `STOPPED_AT`: 現在停留所上。対象index−現在index。当停留所上は0。
- `IN_TRANSIT_TO`: 現在sequenceの停留所へ向かう区間。確定した直前停留所から対象までの区間数。当停留所への区間なら1。
- `INCOMING_AT`: 現在sequenceの停留所へ接近中。数える区間は上記と同じだが、「接近中」と明示して停車中と区別する。
- `current_status` 未設定時は、sequenceがある場合の仕様上の既定値 `IN_TRANSIT_TO` を適用し、未設定だった証拠を残す。川崎市が明示的に配信した値とは扱わない。
- sequence未設定、stop_idとsequenceの不一致、迂回/スキップで隣接関係が不確か、始発より前で直前停留所がない場合は区間数を作らない。対象通過済みは別状態にする。
- 座標の近さやETAから停留所区間を推測しない。区間中央に置くアイコンは模式表現で、移動距離50%の意味にしない。

この定義は公式ナビの「N個前を通過」と同一とは未確認。実データと公式画面の照合後に表示文言をレビューする。
最新のUI指定は設計書9節へ反映した。4方向共通の案内板に先発順3便（系統・行き先・予定出発時刻・のりば）、最下段に現在1位だけの位置を表示する。
順位の変更と位置欄を同時に更新する。順位逆転の実観測、同じ経路での進捗比較可能性は未確認。

## 6. 欠損・例外・更新周期

### 実データで確認した欠損

**まだない。未取得であり「欠損なし」ではない。**

観測ツールが区別するもの:

- timeだけ / delayだけ / 両方 / 両方なし、arrival/departure片側のみ、配信delayと計算delayの不一致。
- 対象trip自体が未配信、対象stopの直接更新がない、同一stop再訪の曖昧さ、NO_DATA/SKIPPED/非SCHEDULED。
- VPなし、trip識別不十分、座標のみ、sequenceなし、status未設定、timestampなし、対象停留所通過済み。
- 非標準/差分feed、頻度運行、運行日不明、カレンダー不一致、改正版とのID不一致。

対象stopへの直接更新がない場合、今回の最小ツールは遅延伝播を実装せず不足として残す。
仕様に従う伝播が可能かを原データの必要範囲で追加確認するまでは、「予測非対応」と結論しない。
当日便が少ない時間帯の0件とデータ非提供も区別する。

### 更新周期の観測計画

初回は静的1回＋RT2種を20秒間隔で4snapshot（通常9要求、約60秒＋通信時間）。失敗時は自動retryしない。引数の上限は6snapshot/間隔15〜60秒。
実行時の適用アクセス条件がこれより厳しい場合はそちらを優先する。

記録対象は取得時刻、feed timestamp、entity種別件数、対象便TU/VP timestamp、内容hashの変化、対象便の停留所状態。
hash変化だけを車両移動と判定しない。20秒の観測間隔を上流の20秒生成周期とも判定しない。
RT2種は順次取得のため完全同時snapshotではなく、両方の取得時刻・上流時刻差を確認する。

| 観測事項 | 結果 |
| --- | --- |
| 初回/最終の実データ取得日時 | 未実施 |
| RT2種の上流timestamp差分 | 未観測 |
| 対象4方向のentity更新・位置進行 | 未観測 |
| 上流遅延・停止・欠落の継続 | 未観測 |
| 15秒pollingの妥当性 | 未判定 |

GO-Aの「安定」は短時間の全線取得成功で代替しない。4方向それぞれの実際の接近便を複数snapshotで観測し、必要なら運行時間帯を変えて追加観測する。無人継続収集や定期実行は追加しない。

## 7. GO判定

| 判定 | 条件 | 今回 |
| --- | --- | --- |
| 共通最低条件 | 4方向すべてでscheduled＋次便判定 | 未実証 |
| GO-A | 4方向でestimated・delay・位置/stopsAwayが安定 | 未判定 |
| GO-B | estimated/delayが成立し、一部位置のみ欠損 | 未判定 |
| GO-C | Staticだけ安定。時刻表P0は可能、RT P0はNO-GO | 未判定 |

現時点は**証拠不足により実装開始NO-GO**。GO-Cと判定できる静的データの証拠もない。
これは「ODPTが取得不能」「川崎市バスがRT非対応」という結論ではない。

P0 Acceptance 1/2/7〜11は本番UI/Worker未実装。3〜6は実データ検証待ち。12は正規経路・条件の初回調査までで、今回の本体取得や本番公開の受入PASSではない。

## 8. 変更・ローカル確認・次の作業

### 変更内容

| ファイル | 目的 |
| --- | --- |
| `bus/.gitignore` | `.dev.vars`・ローカル仮想環境・Pythonキャッシュを除外 |
| `bus/.dev.vars.example` | `ODPT_ACCESS_TOKEN` の空値テンプレート |
| `bus/requirements-validation.txt` | 公式GTFS-Realtime PythonバインディングとProtobufを固定 |
| `bus/validate_odpt.py` | メモリ内取得/解析、固定4方向の証拠集計、取得回数上限、秘密を含まない出力 |
| `bus/test_validate_odpt.py` | 実データを含まない合成試験 |
| `bus/README.md` | 秘密設定と再現手順、観測ツールの限界 |
| `docs/PALURU_BUS_P0_DESIGN.md` | ユーザー指定の案内板UIと先発位置表示を反映 |
| この文書 | 実証/未確認、GO判定、次の観測の記録 |

ローカルだけに空値の `bus/.dev.vars` と `.venv/` を作成した。キー設定はユーザー待ち。
今回追加するのは検証用CLIであり、PALURU API/DTO・GAS・PWA・SW・Build・既存データの変更はない。
追加前からの `gas-family-inbox/FamilyInboxWorkerService.js` の差分は対象外として維持した。
ロールバックは今回の検証ツール/文書差分に限定する。秘密ファイルや他タスクの変更を削除・巻き戻ししない。

### 確認結果

- Python 3.14.3、`gtfs-realtime-bindings==2.2.0`、`protobuf==7.36.1`。インストール先はGit除外済みの `bus/.venv/`。
- 合成試験 **11件PASS**。標柱差分/方向、乗降条件、運行例外・24時超・翌日次便、欠損と0、time優先、過去除外、状態別sequence/欠番、再訪stop、Protobuf結合、日時不一致、秘密出力防止、redirect拒否を確認。
- Python構文確認PASS、`git diff --check` PASS。
- `.dev.vars` と `.venv/` のGit除外、`.dev.vars` 未追跡を確認。
- 正規キー有無の安全な事前確認で停止。実APIの認証/HTTP/データ/4方向の実動作は未検証。
- 既存Repository全体テスト・実ブラウザ/PWA受入・Android実機・本番deployは未実施。合成試験をP0受入の代わりにしない。

### 次の最小ステップ

1. ユーザーがローカル `bus/.dev.vars` へ正規キーを設定する。値はチャットへ送らない。
2. `--check-config` の後、[READMEの検証手順](../bus/README.md)でStatic＋RTを有界観測する。
3. この文書2〜7節を、取得日時・4方向ID/のりば・対象便の最小証拠・欠損・周期の実測値へ更新する。名称だけで本番設定を確定しない。
4. 疎なTU、ID差異、乗り場欠損など観測された差分だけ追加検証する。未観測の穴を推測で埋めない。
5. GO-A/Bと設計レビューを通過後、固定4方向設定＋Kawasaki Adapter＋Bus Core＋独立Worker APIが最小実装単位。その後、共通のスマホ案内板カードとPALURU入口を追加する。
6. ユーザー本人のdeploy後に、4方向・先発順位逆転と位置欄一致・欠損表示・15秒更新/非表示停止・他機能への影響を実ブラウザ/PWAで受入確認する。

現時点では2以降の実データ依存作業と本番実装は保留。

## 9. 正規キーでの実データ検証（2026-09-10追記）

### 9.1 結論・検証範囲

**4方向すべてについて、静的GTFSの正式stop/route/trip/sequence、TripUpdates、VehiclePosition、便単位の結合を実データで確認した。**
時刻4項目のうちscheduled/estimated/delayは4方向で計算できた。未来のetaMinutesは3方向で確認し、登戸発は取得時点でestimatedが過去だったためnullとした。

**判定はNO-GO（現時点の「固定4方向のリアルタイムP0」）。** 正規APIの採用否定ではない。静的時刻表＋次便は4方向で成立しており、RTも一部成立する。GO-A/Bに必要な4方向の未来ETA・位置表示の安定性をこの観測からは証明できない。静的しか取得できないケースでもないのでGO-Cとは分類しない。

- 作業開始時: HEAD `cb30abe92a1fcbffdaf8c574b93506d3dc1edc58`、作業ツリーclean。
- 秘密設定確認: `--check-config` が成功。ファイルの値は出力していない。`.dev.vars` はGit除外・未追跡のまま。
- Static取得成功時刻: 17:23:53、17:26:02、17:32:15、17:34:40の4回。全て2026-09-10、Asia/Tokyo。同一hashを確認。
- RT取得時間帯: **17:23:59〜17:34:45**、15組のsnapshot。対象便のない時刻を補う追加観測と、版混在を避ける修正の確認を含む。
- RT本体30応答、Static本体4取得。最初の2要求は静的転送の拒否/安全な転送先診断で終了した。APIへの要求は合計36回、静的配布先への転送4回。公式のりば案内をブラウザで照合した閲覧はこのAPI回数に含めない。
- APIレスポンス全文はディスクへ保存していない。メモリ内解析と、対象4方向の件数・最大3便/方向/snapshotの抜粋だけを観測。以下はその必要部分の要約。

### 9.2 Staticの実体・正式ID

採用データは [川崎市交通局のODPT Static GTFS](https://ckan.odpt.org/dataset/transportation_bureau_city_of_kawasaki_all_lines)。版は `20260701_20260828`、feed有効期間は `20260701`〜`20270701`。
ZIPは7,423,362 bytes、SHA-256は `dcbd2e24a6fdd46c7af6e7c3145593774bcf4817fde1ea91517b7c1f387860eb`。
stops 1,466行、routes 53行、trips 48,352行、stop_times 955,492行、calendar 76行、calendar_dates 44行。frequenciesは0行。
agency timezoneはAsia/Tokyo。実際の2026-09-10のservice_idをカレンダー照合して次便を抽出した。

#### 固定4方向と標柱の組合せ

| 方向 | 乗車stop_id | 降車stop_id | route_idと公式route_short_name |
| --- | --- | --- | --- |
| 神木本町→登戸 | `184_2` | `362_1` | `10044`＝登０５ |
| 神木本町→溝口駅南口 | `184_1` | `434_5`、一部`434_1` | `10032`＝溝１１、`10033`＝溝１５、`10034`＝溝１６、`10035`＝溝１７、`10036`＝溝１８、`10037`＝溝１９ |
| 登戸→神木本町 | `362_1` | `184_3` | `10044`＝登０５ |
| 溝口駅南口→神木本町 | `434_2` / `434_3` / `434_4` | `184_3` | `434_2`: 10033/10034、`434_3`: 10036、`434_4`: 10032/10035/10037 |

神木本町→溝口の `434_1` を経由する対象は10033/10036のみ。6系統全てが両標柱を使うわけではない。
`434_1` と通常の駅到着側 `434_5` を区別する。公式のりば案内の1番には第三京浜入口方面が表示されるが、個々の対象tripの最終行先はそのtripのstop_headsign/停留所列から確認する。最終行先とユーザーの降車先を混同しない。

名称だけで上記を決めていない。親stop/子標柱、下表の異なる座標、同一trip内の乗車→降車順、pickup/drop_off条件を照合した。方向ごとの全サービス日を含む対象trip数は660 / 3,820 / 660 / 3,716。これは当日運行本数ではない。

| 正式stop名 | 親stop | 子stopと座標（緯度,経度） | のりば表示の証拠 |
| --- | --- | --- | --- |
| 神木本町 | `184` | `184_1`=(35.601256,139.583436)、`184_2`=(35.601563,139.582014)、`184_3`=(35.600749,139.581875) | 公式案内の1番=溝口方面、2番=登05登戸方面、3番=帰りの各系統を照合 |
| 登戸駅（生田緑地口） | `362` | `362_1`=(35.620371,139.569650)、`362_2`=(35.620110,139.570004)、`362_3`=(35.620201,139.570109) | 公式標柱一覧No.1に登05を表示。ただしリンクに「1番のりば」という文字はなく、物理番号は未確認 |
| 溝口駅南口 | `434` | `434_1`=(35.599086,139.610379)、`434_2`=(35.599187,139.610237)、`434_3`=(35.599193,139.609884)、`434_4`=(35.598992,139.609732)、`434_5`=(35.599003,139.610027) | 公式案内の2番=溝15/16、3番=溝18、4番=溝11/17/19を照合 |

親stopは `location_type=1`、子標柱は `location_type=0`。**対象全stopで `platform_code` と `stop_code` は空**。番号をIDの末尾から生成することは禁止し、上記の「番号表示」は別途確認した公式案内の情報として持つ。
登戸はP0対象の `362_1` を他の2標柱と区別できるが、物理番号の表示は未確定。UIで勝手に「1番」と補完しない。

照合元（2026-09-10に公式画面を閲覧。運行データ取得をHTMLへ切り替えたものではない）:

- [神木本町の公式標柱一覧](https://kcbn.bus-navigation.jp/wgsys/wgp/search.htm?existYn=N&from=%E7%A5%9E%E6%9C%A8%E6%9C%AC%E7%94%BA&fromType=1&locale=ja&mapFlag=false&nextDiagramFlag=0&tabName=timetableSignpoleTab)
- [登戸駅（生田緑地口）の公式標柱一覧](https://kcbn.bus-navigation.jp/wgsys/wgp/search.htm?existYn=N&from=%E7%99%BB%E6%88%B8%E9%A7%85%EF%BC%88%E7%94%9F%E7%94%B0%E7%B7%91%E5%9C%B0%E5%8F%A3%EF%BC%89&fromType=1&locale=ja&mapFlag=false&nextDiagramFlag=0&tabName=timetableSignpoleTab)
- [溝口駅南口4番への移設についての交通局告知](https://www.city.kawasaki.jp/820/page/0000169361.html)と公式標柱一覧の系統表を照合。

#### 実便のIDとsequence

全てサービス日 `20260910`。以下は今回実際にTU/VPと照合した例。

| 方向 | trip_id | route_id | 乗車→降車のstop_sequence | 乗車stopの静的発車 |
| --- | --- | --- | --- | --- |
| 神木→登戸 | `4112_01_100001312` | 10044 | 13→20 | 17:27:00 |
| 神木→溝口 | `4112_01_100002613` | 10036 | 17→26 | 17:26:00 |
| 登戸→神木 | `4112_01_100001266` | 10044 | 1→8 | 17:30:00 |
| 溝口→神木 | `4112_01_100002578` | 10035 | 1→10 | 17:26:00 |

静的次便の初回抽出（17:23台）は、神木→登戸17:27/17:42/17:57、神木→溝口17:26/17:27/17:30、登戸→神木17:30/17:45/18:00、溝口→神木17:26/17:29/17:31。4方向で「scheduled＋次便」の最低条件は成立。
実装時もtripごとにsequenceを解決する。神木→登戸だけでも対象sequence13の便と17の便があり、方向単位の固定sequenceは使えない。

### 9.3 RTで実際に埋まった項目

採用候補は [川崎市交通局のODPT GTFS-Realtime](https://ckan.odpt.org/dataset/odpt_transportation_bureau_city_of_kawasaki_all_lines)。全取得でFULL_DATASET。
両配布URLの応答はTUとVPを一緒に含む複合feedだった。Alert entityは今回0件で、Alert非対応を意味しない。

| 方向 | 対象TU数/snapshot | 対象VP数/snapshot | 乗車stopの直接TU数/snapshot | 未来departureを得たsnapshot |
| --- | --- | --- | --- | --- |
| 神木→登戸 | 3〜4 | 3〜4 | 2〜3 | 15/15 |
| 神木→溝口 | 14〜16 | 14〜16 | 10〜12 | 15/15 |
| 登戸→神木 | 2〜3 | 2〜3 | 0〜1 | **0/15** |
| 溝口→神木 | 16〜18 | 16〜18 | 1〜2 | 13/15 |

上表は方向条件を満たすtripの配信数。すでに乗車停留所を通過した便を含むため、全部をUI候補にはしない。

- 対象TUでは `trip_id` / `start_date` / `start_time` / `route_id`、各StopTimeUpdateの `stop_id` / `stop_sequence` / `departure` を実測。**arrivalは対象便の全観測で0件**。
- 乗車stopへ直接配信されたdepartureは延べ220件（同一便・同一feedの反復を含む）。220件全てに `time` と `delay` が両方存在した。timeだけ、delayだけ、両方欠損の直接departureは今回観測していない。
- 乗車stopのイベント以外でもarrivalが無いことを確認。初回の対象trip全体では577 StopTimeUpdates中、departure 577、arrival 0。
- 対象VPで存在した項目は `trip`、`current_stop_sequence`、明示された `current_status`、`stop_id`、latitude/longitude、timestamp。取得できたVPでは、検査対象の位置フィールド欠損は観測しなかった。便の抜粋では `trip_id` / `start_date` / `start_time` / `route_id` も揃っていた。
- `current_status` は **INCOMING_AT / STOPPED_AT / IN_TRANSIT_TOの3種を実測**。仕様に載っているだけという判定ではない。
- 同一trip内のstop_idとsequenceは静的停留所列へ照合できた。便識別はTripDescriptorを使い、FeedEntity.idでは結合していない。
- trip-levelのdelayは例示便で0だったが、当該stopのdeparture.delayは163秒等だった。trip-level delayを当該停留所の遅延の代わりにしない。

### 9.4 scheduled / estimated / delay / etaMinutes の実例

同じdepartureイベント同士で比較。時刻は全て2026-09-10、Asia/Tokyo。秒単位のdelayを保持し、分表示への丸めは本番UIレビューで決める。

| 方向・trip_id末尾 | 取得時刻 | scheduled | estimated（配信time） | 差分delay / 配信delay | etaMinutes |
| --- | --- | --- | --- | --- | --- |
| 神木→登戸 / 100001312 | 17:23:59 | 17:27:00 | 17:29:43 | +163秒 / 163秒、一致 | 6 |
| 神木→溝口 / 100002613 | 17:23:59 | 17:26:00 | 17:29:24 | +204秒 / 204秒、一致 | 6 |
| 登戸→神木 / 100001266 | 17:31:07 | 17:30:00 | 17:30:26 | +26秒 / 26秒、一致 | **null（取得時点で過去）** |
| 溝口→神木 / 100002578 | 17:23:59 | 17:26:00 | 17:26:00 | 0秒 / 0秒、一致 | 3 |

scheduledは静的stop_timesとサービス日から構成した。estimatedはTU.time。delayは差分で検算し、etaMinutesは取得時点の実時計との差をceilした。nullを0へ補完していない。
後半11snapshotの直接departure延べ164件では検算不一致カウンター0件。これは将来の全便で不一致が起きない保証ではない。
time優先・delayのみの場合の加算は合成試験でも確認済みだが、delayのみの川崎実例は今回ない。

**UIへの含意:** 表示可能なのは「発車まで」のETA。arrivalがないので「到着まで6分」「到着が3分遅れ」とは表示しない。停留所への接近状態と発車予測を別項目として扱う。
`trip_headsign` は今回の例で空だった一方、stop_timesの `stop_headsign` には「登戸駅（生田緑地口）」「溝口駅南口」「菅生車庫(蔵敷)」「鷲ヶ峰営業所前(平)」等が入っていた。案内板の行先は乗車地点のstop_headsignを参照する案とする。
上位3便について時刻表順と予測順が逆転する実例は今回未観測。順位入替えの実ブラウザ受入は未実施。

#### 登戸発の時系列と制限

17:30発 `4112_01_100001266` を、発車予定前から発車後まで追った。

| 取得時刻 | 配信time / delay | VP | 判断 |
| --- | --- | --- | --- |
| 17:23:59〜17:29:07の各snapshot | 当該便の乗車stop更新なし | 当該便未観測 | 時刻表17:30は存在。RT未配信を運休/定刻と判断しない |
| 17:30:07 | 17:30:00 / 0秒 | 別URLの旧版との結合で不在に見えた | 下記9.6の版不一致があり、事業者のVP欠損とは断定しない |
| 17:31:07 | 17:30:26 / 26秒 | `362_1`, seq1, STOPPED_AT。位置timestamp17:29:54 | 4方向目の時刻・位置結合は成立。ただしETAは過去なのでnull |
| 17:32:21 | 17:31:29 / 89秒 | `362_1`, seq1, STOPPED_AT。timestamp17:30:55 | 配信timeが取得より前。現在の発車予測と断定できない |
| 17:32:36以降 | 乗車stopの更新が消える | seq2 `202_2`、INCOMING_AT | sequence上は乗車stop通過後として候補から除外。実際の道路上の発車時点は未確認 |

RTのtimeには予測だけでなく実績の意味もある。**過去イベントと、すでに乗車できない過去便は同義ではない。** STOPPED_ATのままtimeが過去の場合、時刻だけで便の発車済みを断定できない。現スクリプトの `futureDepartureCandidate=false` は未来ETA条件の不成立であり、乗車不可の確定ではない。
UIでは「最終観測では停車中・発車時刻更新待ち」等とする設計提案が必要。今回そのUIや無断fallbackは実装していない。

### 9.5 「何停前」「停留所間」の再現可否

**静的停留所順への結合と停留所イベントの再構成は可能。道路上の現在位置に等しい精度の区間表示は未実証。**

| 観測 | 結合できた事実 | 表示可能性・制限 |
| --- | --- | --- |
| 神木→登戸、17:29:07取得 | 100001312、target seq13、current seq11=`261_1`平橋、STOPPED_AT | 観測イベントとして2停前。イベント時刻17:28:35を保持 |
| 神木→溝口、17:29:07取得 | 100002613、target seq17、current seq16=`469_1`向丘中学校下、STOPPED_AT | 観測イベントとして1停前 |
| 同便、17:30:07取得 | current seq17=`184_1`神木本町、INCOMING_AT。直前は`469_1`向丘中学校下 | sequence生差は0だが「向丘中学校下→神木本町へ接近」。提案定義では1停前 |
| 登戸→神木、17:31:07取得 | 100001266、target/current seq1=`362_1`、STOPPED_AT | 始発停留所上という観測、0停前。前運行からの接近区間はこのtripには存在しない |
| 溝口→神木、17:23:59取得 | 100002578、target/current seq1=`434_4`、STOPPED_AT | 始発停留所上という観測、0停前 |

#### IN_TRANSIT_TOの意味は未確定

同じ神木→登戸便 `4112_01_100001312` で以下を観測した。

| VP timestamp | current_stop_sequence / stop_id | status | 緯度・経度（抜粋） | GTFS仕様モデルによる仮のstopsAway |
| --- | --- | --- | --- | --- |
| 17:26:30 | 9 / `245_1` 堰下 | STOPPED_AT | 35.598972, 139.573593 | 4 |
| 17:27:30 | 9 / `245_1` 堰下 | IN_TRANSIT_TO | 35.599190, 139.576172 | 5 |
| 17:28:35 | 11 / `261_1` 平橋 | STOPPED_AT | 35.599766, 139.578247 | 2 |

停車から移動へ変わり座標も変化したが、seq9が据え置かれた。標準の「current sequenceへ向かう区間」という計算をそのまま使うと4停前→5停前と増える。
「川崎では直前停留所を指す」「各フィールドの更新時点がずれる」は仮説であり、今回どちらとも確定していない。座標から別区間を推定して穴埋めもしない。

したがって観測ツールは **IN_TRANSIT_TOのstopsAway/前後区間をnullにする**。理由は `KAWASAKI_TRANSIT_SEQUENCE_MEANING_UNVERIFIED`。仕様モデルの値は `unverifiedSpecStopsAway` として検証用に分離する。
初期A/B観測の `stopsAwayReconstructed` にはこの未確認モデルの値が含まれるため、GO-Aの件数として使わない。C/D観測と最終スクリプトでは抑止済み。
STOPPED_AT/INCOMING_ATも「配信された停留所イベント・その観測時刻」として扱う。GPSと停留所イベントの同時性は未確認であり、秒単位の道路位置の保証には使わない。

### 9.6 欠損・例外・同一snapshotの必要性

1. **乗り場番号欠損:** platform_code/stop_codeが空。公式案内の確認済み表示を設定として別管理する。登戸の物理番号は未確認。
2. **到着イベント欠損:** arrivalがなくdepartureのみ。到着ETA/到着遅延へ読み替えない。
3. **対象stop更新の消失:** 通過後に対象stopの直接更新が消える例を観測。後半の照合可能な欠落はVP上の通過済みと一致した。欠落を運休や0分遅れにしない。
4. **始発前のRT未観測:** 登戸17:30便で未来ETAを取得できなかった。全便・全時間帯の非対応とまでは断定しない。折返し前の別tripの位置を次の便へ流用しない。
5. **原本2URLの版差:** 15組中14組は同一hash。17:30:07ではTU URLのheaderが17:29:55、VP URLは17:29:24で31秒差があった。同時刻の別URL要求でも同じ版とは限らない。
6. **その版差による見かけの欠損:** 上記の登戸当該便で「TUあり/VPなし」に見えた。別版結合だったため、川崎データのVP非配信とは断定しない。
7. **修正と確認:** 両URLがTU/VPを含む複合feedである実測に基づき、観測ツールは新しい方のFULL_DATASET一つを選び、その中でTUとVPを結合する。17:34:45取得で同一snapshot結合が動作した。版差の合成試験もPASS。
8. **観測外:** timeのみ/delayのみ、不正ID、頻度運行、差分feed、NO_DATA/SKIPPED/運休の異常系実例は今回未観測。合成で扱えることと実際の提供状況を分ける。

### 9.7 更新周期・鮮度

| 観測群 | 回数/間隔 | TU headerの連続差分（秒） | VP headerの連続差分（秒） |
| --- | --- | --- | --- |
| A: 17:23:59〜17:24:59 | 4回/20秒 | 0,31,31 | 0,31,31 |
| B: 17:26:07〜17:31:07 | 6回/60秒 | 62,63,62,62,32 | 62,63,62,31,63 |
| C: 17:32:21〜17:33:06 | 4回/15秒 | 31,0,31 | 31,0,31 |
| D: 17:34:45 | 1回、同一版結合確認 | 単発 | 単発 |

ヘッダー差分はおおむね31〜32秒の更新を示唆する。60秒観測では更新を飛び越えるため62〜63秒が見える。これは仕様上の保証周期ではなく、この時間帯の実測。
取得時のfeed ageは **13〜52秒**。方向ごとの最大3便の抜粋に含まれるVP ageは **約16.9〜111.4秒**。全線全車両の最大値ではない。
同じfeedでもheaderとTU/VP timestampは異なり、freshなheaderを車両の観測時刻へ代用できない。timestampが据え置かれたまま予測timeが更新される例もあった。
15秒pollingで毎回新しい位置が届くわけではない。UI15秒案を維持する場合も、上流の取得頻度・cacheと位置の有効期限はこの実測を踏まえて設計レビューする。今回cache/SW/Workerの設定は変更していない。

### 9.8 GO判定と次に実装すべき最小構成

| 判定 | 今回の根拠・不足 |
| --- | --- |
| 最低条件（4方向scheduled＋次便） | **成立**。service dateとstop順を照合し、3便ずつ取得 |
| GO-A | **未達**。移動中区間の意味が未確定、4方向の未来ETA安定を証明できない |
| GO-B | **保留**。3方向で未来ETAを確認したが、登戸発は0/15。位置だけ欠けるケースとして承認できる状態ではない |
| GO-C | 静的時刻表は成立するが、実際にはRTも取得・結合できるので「Staticしか安定しない」とは結論していない |
| 総合 | **NO-GO（現時点の4方向RT P0開始判定）**。正規データ経路は継続採用候補。P0完成・本番受入の判断ではない |

次に必要な最小作業は、登戸発の別便/時間帯で発車前のTU coverageを追加確認し、IN_TRANSIT_TOの意味・停留所イベントとGPSの時点差を確認すること。過去timeかつSTOPPED_ATの扱いも設計レビュー対象とする。問い合わせ等の外部送信は今回行っていない。

その後の最小実装案は次の範囲に限る。

1. 確認した標柱ID・route_id・公式のりば表示の出典を持つ固定4方向設定。trip/sequenceは静的GTFSから都度解決する。
2. Kawasaki AdapterでStatic＋同一複合RT snapshotを結合し、departure時刻・欠損理由・元の観測時刻を共通DTOへ正規化する。IN_TRANSIT_TO区間は確認前ならnull。
3. Bus Coreで運行日、通過/運休/欠損、予測順、最大3便、位置の鮮度と1位の便の一致を決定的に処理する。
4. 独立Workerの読取APIと短期cache。GASを通さない。キーはWorker Secret、原本feedをPWAへ返さない。
5. 設計判断後に共通案内板カードとPALURU入口。停車中・未来ETA・予定のみ・不明を区別する。本フェーズでは着手していない。

### 9.9 変更・テスト・残る受入

- 変更ファイル: `bus/validate_odpt.py`、`bus/test_validate_odpt.py`、`bus/README.md`、この文書。
- 観測ツールの変更理由: 実際の静的配布redirect、RT別版の混在、IN_TRANSIT_TOの意味の不確実さに対応。stop_headsign・位置の停留所名・欠損/検算カウンターも要約へ追加した。本番Provider/Core/UIではない。
- 静的redirectは確認したHTTPSホストと指定版パスへ1回のみ。元のAPIキー/認証ヘッダーは転送しない。署名値、認証付きURL、例外本文は出力しない。
- 合成試験 **14件PASS**。新たに配布先制限/キー転送禁止、IN_TRANSIT_TOの断定抑止、異なる版を混ぜない選択を確認。
- Python構文確認・`git diff --check` PASS。変更4ファイル内の実トークン/URL符号化した値の混入検査は検出なし。空値exampleと秘密ファイルのGit除外・未追跡も確認した。
- 実API取得・実データ結合: 上記の範囲で確認。14件の合成PASSと混同しない。
- 本番UI/API、GAS、PWA/SW、Build、既存データ、Cloudflare Secret/設定は変更なし。deploy/commit/push未実施。
- Repository全体の既存テスト、PALURU実ブラウザ/PWA受入、Android実機は未実施。公式のりば案内の閲覧をPALURUの実機受入として数えない。
- 受入の残り: 登戸発の発車前coverage、区間意味、鮮度期限、実際の順位逆転、停車中の過去time、15秒更新/非表示停止、他機能への非波及、ユーザー本人のdeploy後の実PWA確認。

## 10. 2026-09-10 P0実装中の追加観測

9節のNO-GOは初回観測時の履歴。以後のユーザー決定は **P0実装着手GO、位置UIはFeature Gate OFF**。Static/scheduled/estimated/delay/VP結合を使用し、未来ETAは取得できる便だけ表示する。更新周期は30秒に変更された。

### 10.1 観測条件と登戸発の未来ETA

- 日時: 2026-09-10 18:16〜18:18に3回・60秒間隔、18:44:30〜18:47:00に6回・30秒間隔（Asia/Tokyo）。いずれも静的版20260828。原本とキーは保存せず、既存の検証スクリプトによる対象便要約だけを確認。
- 18:18の登戸18:15便は予測18:17:48、停車中だが観測時には過去。ETAはnullだった。
- **18:45:30の観測で登戸発の未来ETAを1件実証**。短時間の1便の確認であり、常時coverageや全便の安定性は証明しない。

| 項目 | 実データ |
| --- | --- |
| trip_id / 運行日 | `4112_01_100001268` / `20260910` |
| route_id / 系統 | `10044` / 登０５ |
| 乗車→降車 | `362_1` 登戸駅（生田緑地口）seq 1 → `184_3` 神木本町seq 8 |
| start_time / 行先 | 18:46:00 / 菅生車庫(蔵敷) |
| feed timestamp | 18:45:21 |
| TU/VP timestamp | ともに18:45:12 |
| scheduled departure | 18:46:00 |
| reported departure.time | 18:46:00 |
| reported departure.delay | 0秒（明示値） |
| estimated / 検算delay / etaMinutes | 18:46:00 / 0秒・一致 / 1分 |
| VP | stop_id `362_1`、current_stop_sequence 1、STOPPED_AT、観測時age約18.8秒 |
| arrivalイベント | 欠損。departureとの混用なし |

同便は18:46:00の観測ではtimeが過去になりETA=null。18:46:30にはestimated 18:46:23、delay23秒、依然STOPPED_ATでETA=nullだった。実装では「予測更新待ち」とし、過去ETAや未確認の出発済み判定を出さない。

18:45:30の同一観測では、未来departureを再構成した候補が神木→登戸1件、神木→溝口10件、登戸→神木1件、溝口→神木3件あった。正式stop/route設定は9.2〜9.3節のままで、trip_id/sequenceを設定へ固定していない。

### 10.2 周期・位置・残る欠損

- trip_update feedの6回のtimestamp差: **31, 31, 31, 32, 31秒**。
- vehicle endpointの差: 31, 31, 31, 0, 32秒。5・6回目はtrip_updateより1版古い。別endpoint間を混ぜない構造が必要で、実装はTU/VPを含む単一trip_update FULL_DATASETを使用する。
- IN_TRANSIT_TOは神木→溝口、登戸→神木、溝口→神木で追加観測できた。今回の神木→登戸ではSTOPPED_AT/INCOMING_ATのみ。9.7節の同じsequenceでの停車→移動の問題を解消する新たな意味の確証は得ていない。
- STOPPED_AT at boardingは同一trip/date/stop/sequenceで照合できる。4方向のstopsAway・previousStop/nextStopの整合性はまだ未確認。APIはposition.supported=false、全位置値null、UIに位置DOMなし。
- 登戸発の発車前RTは今回1件。便ごとの未配信、通過後のtarget update欠損、arrival欠損、feed/便/車両のtimestamp差は残る。欠損から定時運行を生成しない。

### 10.3 実装へ渡す判定

| 対象 | 現状 |
| --- | --- |
| Static固定4方向 / scheduled | GO |
| estimated / delay | 実データで再構成、欠損時は静的fallback |
| etaMinutes | 4方向の未来値を観測済み。futureかつ鮮度内だけ表示する条件付きGO |
| VPとTripの結合 | GO。UIの区間表現とは別 |
| stopsAway / 停留所間 | 検証継続、Feature Gate OFF |
| P0実装 | ユーザー判断によるGO。時刻中心のローカル実装あり |
| GO-A / 安定GO-B | 位置解釈・長時間安定性を未実証のため未達。位置OFFの時刻P0を継続 |
| 本番deploy / 実PWA受入 | 未実施。詳細は [実装記録](PALURU_BUS_P0_IMPLEMENTATION.md) |

この追加観測を以て位置GateをONにはしていない。実装・合成テスト・ローカル実データ・本番受入を区別する。
